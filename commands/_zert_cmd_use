_zert_cmd_use() {
  local framework="$1"
  shift

  case "$framework" in
    ohmyzsh) _zert_use_ohmyzsh "$@" ;;
    prezto)  _zert_use_prezto "$@" ;;
    *)
      _zert_ui_error "Unsupported framework: $framework"
      _zert_ui_log "Available: ohmyzsh, prezto"
      return 1
      ;;
  esac
}
