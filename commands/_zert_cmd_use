# Command to handle "use" syntax for ohmyzsh/prezto
_zert_cmd_use() {
  local input="$1"

  if [[ -z "$input" ]]; then
    _zert_ui_error "Usage: zert use <ohmyzsh|prezto>/<path>"
    return 1
  fi

  _zert_ui_log "Use command: $input"
  _zert_ui_log "Not yet implemented"
}
