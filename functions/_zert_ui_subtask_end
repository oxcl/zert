_zert_ui_subtask_end() {
  [[ "$__ZERT_TASK_ENDED" == "1" ]] && return 0
  local subtask_index="$1"
  local subtask_status="$2"

  print -u4 "subtask_${subtask_index}_status=$subtask_status"
  if [[ "$subtask_status" == "ok" ]]; then
    print -u4 "log_clear=1"
  fi
}