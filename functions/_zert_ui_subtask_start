_zert_ui_subtask_start(){
    [[ "$__ZERT_TASK_ENDED" == "1" ]] && return 0
    local subtask_index="$1" subtask_title="$2"
    echo "subtask_${subtask_index}_title=$subtask_title" >&4
    echo "subtask_${subtask_index}_status=ongoing" >&4
}