_zert_ui_subtask_log() {
  local max_lines="${1:-4}"

  print -u4 "log_max=$max_lines"

  stdbuf -o0 -i0 tr '\r' '\n' | while IFS= read -r line; do
    [[ -z "$line" ]] && continue

    print -u4 "log_line=$(printf '%s' "$line" | base64)"
    print -n -u4 ''
  done
}
