_zert_plugin_compile() {
  local plugin_dir="$1"

  [[ ! -d "$plugin_dir" ]] && return 1

  local -a zsh_files
  zsh_files=("$plugin_dir"/**/*.zsh(N))

  for zsh_file in "${zsh_files[@]}"; do
    [[ -f "$zsh_file" ]] || continue
    zcompile "$zsh_file.zwc" "$zsh_file" 2>&1
  done
}
